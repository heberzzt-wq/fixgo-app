name: archived mission-contract authority closeout

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Historical workflow marker
        run: echo "MISSION_CONTRACT_AUTHORITY_CLOSEOUT_RETIRED_BY_V142=true"
