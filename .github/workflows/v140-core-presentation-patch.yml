name: archived v140 core presentation patch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Historical workflow marker
        run: echo "V140_CORE_PRESENTATION_PATCH_RETIRED_BY_V142=true"
