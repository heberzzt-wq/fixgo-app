name: v133-reel-fidelity-no-lexical-regex-apply

on:
  push:
    branches:
      - v94-media-v4n-negative-claims
    paths:
      - .github/scripts/v133-reel-fidelity-no-lexical-regex.py
      - .github/workflows/v133-reel-fidelity-no-lexical-regex-apply.yml

permissions:
  contents: write

jobs:
  apply-v133-zero-regex:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout isolated branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: v94-media-v4n-negative-claims

      - name: Guard branch and Functions baseline
        shell: bash
        run: |
          set -euo pipefail
          test "$(git branch --show-current)" = "v94-media-v4n-negative-claims"
          ! git diff --name-only a09d76c5d42117aa88630f7b0d5fee75290525b2..HEAD | grep -E '^functions/'

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Apply zero lexical regex correction
        run: python .github/scripts/v133-reel-fidelity-no-lexical-regex.py

      - name: Assert Functions untouched
        shell: bash
        run: |
          set -euo pipefail
          ! git diff --name-only | grep -E '^functions/'

      - name: Syntax check
        run: node --check gestia-core/jarvis/jarvis.multifunction.planner.js

      - name: Focal architecture plus v133
        run: |
          node --test \
            tests/single-semantic-authority.test.mjs \
            tests/jarvis-reel-mission-fidelity-v133.test.mjs \
            tests/jarvis-semantic-planner.test.cjs

      - name: Reject whitespace errors
        run: git diff --check

      - name: Commit functional correction
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add gestia-core/jarvis/jarvis.multifunction.planner.js
          git commit -m "[v133] fix(adj): preserve mission fidelity without lexical regex"
          git push origin HEAD:v94-media-v4n-negative-claims

      - name: Correction marker
        run: |
          echo "V133_RESEARCH_FIDELITY_SEGMENTER=true"
          echo "V133_ZERO_LEXICAL_REGEX=true"
          echo "V133_FUNCTIONS_UNCHANGED=true"
