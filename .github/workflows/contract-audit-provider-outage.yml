name: archived contract-audit provider outage

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Historical workflow marker
        run: echo "CONTRACT_AUDIT_PROVIDER_OUTAGE_RETIRED_BY_V142=true"
