name: v134-tiktok-media-diagnostic

on:
  push:
    branches:
      - v94-media-v4n-negative-claims
    paths:
      - .github/workflows/v134-tiktok-media-diagnostic.yml

permissions:
  contents: read

jobs:
  probe-research-media:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout isolated branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: v94-media-v4n-negative-claims

      - name: Guard branch and Functions
        shell: bash
        run: |
          set -euo pipefail
          test "${GITHUB_REF_NAME}" = "v94-media-v4n-negative-claims"
          ! git diff --name-only 09d7333bc7b6f86d9698c0a69c8c94e1e0717817..HEAD | grep -E '^functions/'

      - name: Locate Chrome
        id: chrome
        shell: bash
        run: |
          set -euo pipefail
          CHROME="$(command -v google-chrome || command -v chromium || command -v chromium-browser || true)"
          test -n "$CHROME"
          echo "path=$CHROME" >> "$GITHUB_OUTPUT"
          "$CHROME" --version

      - name: Render verified Uber Eats source
        shell: bash
        env:
          CHROME: ${{ steps.chrome.outputs.path }}
        run: |
          set -euo pipefail
          "$CHROME" \
            --headless=new \
            --no-sandbox \
            --disable-gpu \
            --disable-dev-shm-usage \
            --autoplay-policy=no-user-gesture-required \
            --virtual-time-budget=12000 \
            --dump-dom \
            'https://www.ubereats.com/mx/store/takos-el-dorado-cancun/DmKfCVrVW9W46Zx3LBI8jw' \
            > "$RUNNER_TEMP/uber-rendered.html" 2> "$RUNNER_TEMP/chrome-uber.stderr" || true
          test -s "$RUNNER_TEMP/uber-rendered.html"
          echo "UBER_RENDERED_BYTES=$(wc -c < "$RUNNER_TEMP/uber-rendered.html")"

      - name: Inspect rendered Uber Eats media without downloading it
        shell: bash
        run: |
          set -euo pipefail
          node --input-type=module <<'NODE'
          import fs from 'node:fs';
          import { __test as webMediaTest } from './nexo-web-media-bridge.js';
          const sourceUrl = 'https://www.ubereats.com/mx/store/takos-el-dorado-cancun/DmKfCVrVW9W46Zx3LBI8jw';
          const html = fs.readFileSync(process.env.RUNNER_TEMP + '/uber-rendered.html', 'utf8');
          const candidates = webMediaTest.mediaCandidates(html, sourceUrl);
          const images = candidates.filter(item => item.kind === 'image');
          const videos = candidates.filter(item => item.kind === 'video');
          console.log('UBER_RENDERED_DOM_MEDIA_PROBE', JSON.stringify({
            htmlBytes: Buffer.byteLength(html),
            counts: { images: images.length, videos: videos.length, total: candidates.length },
            challengeSignals: ['captcha','access denied','forbidden'].filter(term => html.toLowerCase().includes(term)),
            samples: candidates.slice(0, 16).map(item => ({
              kind: item.kind,
              sourceTag: item.sourceTag,
              mediaRole: item.mediaRole,
              host: new URL(item.url).hostname,
              path: new URL(item.url).pathname.slice(0, 180)
            }))
          }, null, 2));
          if (candidates.length === 0) process.exitCode = 2;
          NODE
