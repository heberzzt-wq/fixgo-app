{
  "jobId": "v142-humo17-multireference-runner-20260909-440",
  "operation": "patch",
  "humanApproved": true,
  "patch": {
    "file": "scripts/jarvis-local-video-wan22.py",
    "dryRun": false,
    "expectedMatches": 1,
    "search": "    for file, sha in ((reference, job[\"referenceSha256\"]), (audio, job[\"audioSha256\"])):",
    "replace": "    input_pairs = list(zip(reference_files, reference_shas)) + [(audio, str(job[\"audioSha256\"]).strip().lower())]\n    for file, sha in input_pairs:"
  }
}
