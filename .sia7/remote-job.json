{
  "jobId": "v142-humo17-worker-env-20260908-295",
  "operation": "patch",
  "humanApproved": true,
  "patch": {
    "file": "jarvis-github-worker.js",
    "dryRun": false,
    "expectedMatches": 1,
    "search": "function runLocalProcess(command, args = [], { cwd = REPO_ROOT, timeoutMs = 120000 } = {}) {\n    return new Promise(resolve => {\n        const child = spawn(command, args, {\n            cwd,\n            shell: false,\n            windowsHide: true,\n            stdio: [\"ignore\", \"pipe\", \"pipe\"],\n            env: { ...process.env }\n        });",
    "replace": "function runLocalProcess(command, args = [], { cwd = REPO_ROOT, timeoutMs = 120000, env = process.env } = {}) {\n    return new Promise(resolve => {\n        const child = spawn(command, args, {\n            cwd,\n            shell: false,\n            windowsHide: true,\n            stdio: [\"ignore\", \"pipe\", \"pipe\"],\n            env: { ...env }\n        });"
  }
}
