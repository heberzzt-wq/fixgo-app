{
  "jobId": "v142-humo-volume-fallback-validation-20260907-124",
  "operation": "patch",
  "humanApproved": true,
  "patch": {
    "file": "jarvis-fs-bridge.js",
    "dryRun": false,
    "expectedMatches": 1,
    "search": "    if (!created.id || created.name !== name || created.dataCenterId !== dataCenterId ||",
    "replace": "    if (!created.id || !created.name.startsWith(prefix) || !eligibleDc.has(created.dataCenterId) ||"
  }
}
