{
  "jobId": "v142-humo-official-ephemeral-staging-20260907-68",
  "operation": "patch",
  "humanApproved": true,
  "patch": {
    "file": "jarvis-fs-bridge.js",
    "dryRun": false,
    "expectedMatches": 1,
    "search": "        const windowsDoubleQuote = value => `\\\"${String(value).replace(/\\\"/g, '\\\"\\\"')}\\\"`;\r\n        const sourceItems = [...relativeFiles, \\"HuMo\\"].map(windowsDoubleQuote).join(\\" \\" );\r\n        const sourceCommand = `cmd.exe /d /s /c \\\"\\\"%SystemRoot%\\\\\\\\System32\\\\\\\\tar.exe\\\" -cf - -C ${windowsDoubleQuote(authority.cacheRoot)} ${sourceItems}\\\"`;\r\n        const destinationCommand = `rm -rf ${posixShellSingleQuote(destinationRoot)} && mkdir -p ${posixShellSingleQuote(destinationRoot)} && tar -xf - -C ${posixShellSingleQuote(destinationRoot)}`;\r\n        const provisionedAtMs = Date.parse(String(state?.provisionedAt || state?.createdAt || \\"\\"));\r\n        const maximumAuthorizedSeconds = Number(state?.maximumAuthorizedSeconds || 0);\r\n        if (!Number.isFinite(provisionedAtMs) || !Number.isFinite(maximumAuthorizedSeconds) || maximumAuthorizedSeconds <= 0) {\r\n            throw new Error(\\"HUMO_EPHEMERAL_BUDGET_DEADLINE_REQUIRED\\");\r\n        }\r\n        const budgetDeadlineMs = provisionedAtMs + (maximumAuthorizedSeconds * 1000);\r\n        const cleanupReserveMs = 120 * 1000;\r\n        const remainingBudgetMs = () => Math.floor(budgetDeadlineMs - Date.now() - cleanupReserveMs);\r\n        const transferTimeoutMs = remainingBudgetMs();\r\n        if (transferTimeoutMs <= 0) throw new Error(\\"HUMO_EPHEMERAL_BUDGET_EXHAUSTED\\");\r\n        await pipeHuMoLanTarToPod(authority, state, sourceCommand, destinationCommand, {\r\n            spawnImpl,\r\n            timeoutMs: Math.min(60 * 60 * 1000, transferTimeoutMs)\r\n        });",
    "replace": "        const contractFiles = RUNPOD_HUMO_CACHE_BASE.requiredFiles.map(item => ({\r\n            path: String(item.path || \\"\\"),\r\n            repository: String(item.repository || \\"\\"),\r\n            revision: String(item.revision || \\"\\"),\r\n            sourcePath: String(item.sourcePath || \\"\\"),\r\n            bytes: Number(item.bytes || 0),\r\n            sha256: String(item.sha256 || \\"\\")\r\n        }));\r\n        if (\r\n            contractFiles.length !== Number(transferPlan.assetCount || 0) ||\r\n            Number(RUNPOD_HUMO_CACHE_BASE.totalBytes || 0) !== Number(transferPlan.transferBytes || 0) ||\r\n            contractFiles.some((item, index) =>\r\n                item.path !== relativeFiles[index] || !item.repository || !item.revision || !item.sourcePath ||\r\n                !(item.bytes > 0) || !/^[a-f0-9]{64}$/i.test(item.sha256)\r\n            )\r\n        ) {\r\n            throw new Error(\\"HUMO_OFFICIAL_EPHEMERAL_CONTRACT_INVALID\\");\r\n        }\r\n        const provisionedAtMs = Date.parse(String(state?.provisionedAt || state?.createdAt || \\"\\"));\r\n        const maximumAuthorizedSeconds = Number(state?.maximumAuthorizedSeconds || 0);\r\n        if (!Number.isFinite(provisionedAtMs) || !Number.isFinite(maximumAuthorizedSeconds) || maximumAuthorizedSeconds <= 0) {\r\n            throw new Error(\\"HUMO_EPHEMERAL_BUDGET_DEADLINE_REQUIRED\\");\r\n        }\r\n        const budgetDeadlineMs = provisionedAtMs + (maximumAuthorizedSeconds * 1000);\r\n        const cleanupReserveMs = 120 * 1000;\r\n        const remainingBudgetMs = () => Math.floor(budgetDeadlineMs - Date.now() - cleanupReserveMs);\r\n        const stageTimeoutMs = remainingBudgetMs();\r\n        if (stageTimeoutMs <= 0) throw new Error(\\"HUMO_EPHEMERAL_BUDGET_EXHAUSTED\\");\r\n        const stagePayloadBase64 = Buffer.from(JSON.stringify({\r\n            files: contractFiles,\r\n            maxWorkers: Number(RUNPOD_HUMO_CACHE_BASE.downloadTools?.maxWorkers || 2)\r\n        }), \\"utf8\\").toString(\\"base64\\");\r\n        const stageProgram = [\r\n            \\"import base64,concurrent.futures,json,os,pathlib,sys\\",\r\n            \\"payload=json.loads(base64.b64decode(sys.argv[1]).decode('utf-8'))\\",\r\n            \\"root=pathlib.Path(sys.argv[2])\\",\r\n            \\"from huggingface_hub import hf_hub_download\\",\r\n            \\"def stage(item):\\",\r\n            \\"    target=root/item['path']; target.parent.mkdir(parents=True,exist_ok=True)\\",\r\n            \\"    if target.is_file() and target.stat().st_size==int(item['bytes']): return\\",\r\n            \\"    partial=root/'.partial'/item['repository'].replace('/','--')/item['revision']\\",\r\n            \\"    downloaded=pathlib.Path(hf_hub_download(repo_id=item['repository'],revision=item['revision'],filename=item['sourcePath'],local_dir=str(partial)))\\",\r\n            \\"    metadata=partial/'.cache'/'huggingface'/'download'/(item['sourcePath']+'.metadata')\\",\r\n            \\"    if not metadata.is_file() or metadata.read_text().splitlines()[0] != item['revision']: raise SystemExit('HUMO_OFFICIAL_DOWNLOAD_REVISION_MISMATCH:'+item['path'])\\",\r\n            \\"    if not downloaded.is_file() or downloaded.stat().st_size != int(item['bytes']): raise SystemExit('HUMO_OFFICIAL_DOWNLOAD_SIZE_MISMATCH:'+item['path'])\\",\r\n            \\"    os.replace(downloaded,target)\\",\r\n            \\"with concurrent.futures.ThreadPoolExecutor(max_workers=int(payload['maxWorkers'])) as pool: list(pool.map(stage,payload['files']))\\",\r\n            \\"print(json.dumps({'ok':True,'downloaded':len(payload['files'])},separators=(',',':')))\\"\r\n        ].join(\\"\\n\\");\r\n        const sourceRevision = String(transferPlan.sourceRevision || RUNPOD_HUMO_CACHE_BASE.sourceRevision || \\"\\");\r\n        const sourceRepository = String(RUNPOD_HUMO_CACHE_BASE.sourceRepository || \\"Phantom-video/HuMo\\");\r\n        const remoteRepository = String(transferPlan.destinationSourceRepository || `${destinationRoot}/HuMo`);\r\n        const stageCommand = [\r\n            \\"set -euo pipefail\\",\r\n            \\"export DEBIAN_FRONTEND=noninteractive\\",\r\n            `ROOT=${posixShellSingleQuote(destinationRoot)}`,\r\n            `REPO=${posixShellSingleQuote(remoteRepository)}`,\r\n            \\"if ! command -v git >/dev/null 2>&1 || ! python3 -m venv --help >/dev/null 2>&1; then apt-get update -qq; apt-get install -y -qq git python3-venv ca-certificates; fi\\",\r\n            \\"mkdir -p \\\"$ROOT\\\"\\",\r\n            `if ! test -d \\\"$REPO/.git\\\"; then git clone --filter=blob:none https://github.com/${sourceRepository}.git \\\"$REPO\\\"; fi`,\r\n            `git -C \\\"$REPO\\\" fetch --depth 1 origin ${sourceRevision}`,\r\n            `git -C \\\"$REPO\\\" checkout --detach ${sourceRevision}`,\r\n            \\"STAGE_VENV=\\\"$ROOT/.official-stage-venv\\\"\\",\r\n            \\"test -x \\\"$STAGE_VENV/bin/python\\\" || python3 -m venv \\\"$STAGE_VENV\\\"\\",\r\n            `\\\"$STAGE_VENV/bin/python\\\" -m pip install --disable-pip-version-check --no-input 'huggingface_hub==${RUNPOD_HUMO_CACHE_BASE.downloadTools.huggingfaceHub}' 'hf-xet==${RUNPOD_HUMO_CACHE_BASE.downloadTools.hfXet}'`,\r\n            \\"export HF_HOME=\\\"$ROOT/.cache/huggingface\\\" HF_HUB_CACHE=\\\"$ROOT/.cache/huggingface/hub\\\" HF_XET_CACHE=\\\"$ROOT/.cache/huggingface/xet\\\" HF_HUB_DISABLE_TELEMETRY=1\\",\r\n            \\"export HF_XET_CHUNK_CACHE_SIZE_BYTES=0 HF_XET_SHARD_CACHE_SIZE_LIMIT=0 HF_HUB_DOWNLOAD_TIMEOUT=120\\",\r\n            `\\\"$STAGE_VENV/bin/python\\\" -c ${posixShellSingleQuote(stageProgram)} ${posixShellSingleQuote(stagePayloadBase64)} \\\"$ROOT\\\"`\r\n        ].join(\\"\\n\\");\r\n        const stagedDownload = await spawnCaptured(openSshExecutable(), huMoPodSshArgs(state, stageCommand), {\r\n            spawnImpl,\r\n            timeoutMs: Math.min(45 * 60 * 1000, stageTimeoutMs),\r\n            maxBytes: 8 * 1024 * 1024\r\n        });\r\n        const stageLine = stagedDownload.stdout.split(/\\r?\\n/).filter(Boolean).at(-1);\r\n        const stageReceipt = JSON.parse(stageLine);\r\n        if (stageReceipt?.ok !== true || Number(stageReceipt.downloaded || 0) !== contractFiles.length) {\r\n            throw new Error(\\"HUMO_OFFICIAL_EPHEMERAL_STAGE_FAILED\\");\r\n        }"
  }
}
