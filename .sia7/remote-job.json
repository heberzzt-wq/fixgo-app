{
  "jobId": "v142-humo17-multireference-runner-20260909-442",
  "operation": "patch",
  "humanApproved": true,
  "patch": {
    "file": "scripts/jarvis-local-video-wan22.py",
    "dryRun": false,
    "expectedMatches": 1,
    "search": "    image = torch.from_numpy(np.asarray(prepared_image, dtype=np.float32) / 255.0).unsqueeze(0)",
    "replace": "    image = torch.cat([torch.from_numpy(np.asarray(ImageOps.pad(Image.open(reference_item).convert(\"RGB\"), (832, 480), method=Image.Resampling.LANCZOS, color=(255, 255, 255)), dtype=np.float32) / 255.0).unsqueeze(0) for reference_item in reference_files], dim=0)\n    reference_preprocessing = {**reference_preprocessing, \"count\": len(reference_files), \"sha256s\": reference_shas}"
  }
}
