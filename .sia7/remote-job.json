{
  "jobId": "v142-humo17-quality-artifact-root-bounds-20260908-334",
  "operation": "patch",
  "humanApproved": true,
  "patch": {
    "file": "jarvis-fs-bridge.js",
    "dryRun": false,
    "expectedMatches": 1,
    "search": "            if (entry.isDirectory()) stack.push(target); else if (entry.isFile() && [\".json\", \".wav\"].includes(path.extname(target).toLowerCase())) files.push(target);\n            if (files.length > 200) throw new Error(\"HUMO17_QUALITY_LOCAL_SCAN_LIMIT\");",
    "replace": "            if (entry.isDirectory()) stack.push(target); else if (entry.isFile() && [\".json\", \".wav\"].includes(path.extname(target).toLowerCase()) && fs.statSync(target).size <= 16 * 1024 * 1024) files.push(target);\n            if (files.length > 1000) throw new Error(\"HUMO17_QUALITY_LOCAL_SCAN_LIMIT\");"
  }
}
