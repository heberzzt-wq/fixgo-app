{
  "jobId": "v142-humo-local-poll-guard-20260906-26",
  "operation": "patch",
  "humanApproved": true,
  "patch": {
    "file": "jarvis-local-video-engine.js",
    "dryRun": false,
    "expectedMatches": 1,
    "search": "                    if (!state.networkVolumeId || state.networkVolumeId !== networkVolumeId || !state.networkVolumeRetentionAuthorized) {",
    "replace": "                    if (state.networkVolumeId && (state.networkVolumeId !== networkVolumeId || !state.networkVolumeRetentionAuthorized)) {"
  }
}
