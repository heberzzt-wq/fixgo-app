jarvis, realiza una auditoría cognitiva completa del runtime gestiaos.

objetivo:

1. analiza el estado actual del sistema.

2. detecta módulos críticos conectados al runtime.

3. identifica dependencias sensibles relacionadas con:

   * firebase.js
   * operations-executor.engine.js
   * gestia-terminal
   * semantic matrix
   * authority registry
   * runtime snapshots
   * repo hub
   * filesystem bridge

4. genera un mapa de riesgo técnico:

   * archivos críticos
   * archivos seguros
   * capas acopladas
   * dependencias circulares
   * riesgo de runtime crash
   * riesgo de corrupción de estado

5. ejecuta análisis semántico completo del repositorio:

   * conceptos dominantes
   * módulos más conectados
   * zonas cognitivas activas
   * ventanas de contexto activas
   * estado emocional detectado
   * prioridades inferidas

6. simula una operación autónoma de modificación real:

   * crear patch
   * validar governance
   * validar safezone
   * calcular impacto
   * generar rollback
   * generar snapshot
   * validar dependencias
   * generar telemetry

7. intenta preparar una propuesta real de modificación para:
   modules/runtime_probe_v1.js

contenido esperado:

```js
window.runtime_probe = {
    active: true,
    timestamp: date.now(),
    cognition: "online"
};

console.log(
    "🧠 runtime probe active"
);
```

8. no ejecutar escritura física todavía.

9. solo generar:

   * analysis
   * proposal
   * dependency graph
   * semantic report
   * governance report
   * execution preview
   * patch preview
   * rollback strategy

10. reporta finalmente:

* nivel de autonomía actual
* porcentaje estimado de self-modifying capability
* readiness para autonomous engineering
* blockers principales
* módulos faltantes
* capacidad real del execution fabric
* estado del cognitive runtime
* capacidad de repo mutation real
* si existe o no conciencia semántica persistente
* si el runtime puede evolucionar sin intervención humana

modo:
supreme_deep_audit

prioridad:
maximum_runtime_introspection